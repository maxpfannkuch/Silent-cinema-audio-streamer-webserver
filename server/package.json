{
  "name": "silent-cinema-sync-server",
  "version": "1.0.0",
  "private": true,
  "description": "WebSocket sync server for a local-audio Silent Cinema web app.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
